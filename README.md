## 📋 Описание проекта

Веб-приложение для управления учётными записями пользователей, реализованное с использованием контейнерных технологий.

### Стек технологий:
- **Backend**: FastAPI (Python)
- **Frontend**: HTML/CSS/JavaScript + Nginx
- **База данных**: PostgreSQL
- **Админка БД**: Adminer
- **Контейнеризация**: Docker, Docker Compose
- **CI/CD**: GitHub Actions

## 🏗️ Архитектура

Приложение состоит из 5 контейнеров:

1. **postgres** - база данных PostgreSQL
2. **backend** - FastAPI REST API
3. **frontend** - веб-интерфейс на Nginx
4. **adminer** - веб-интерфейс для управления БД
5. **redis** - кеширование и защита от брутфорса

Все контейнеры связаны через внутреннюю сеть `user_management_network`.

## 🔐 Безопасность

- Пароли хешируются с помощью **bcrypt**
- **Защита от брутфорса**: блокировка после 5 неудачных попыток входа (через Redis)
- CORS настроен для взаимодействия frontend/backend
- Роли пользователей: `user` и `admin`

## 📊 База данных

### Таблица `users`:
- `id` - первичный ключ
- `username` - уникальное имя пользователя
- `email` - уникальный email
- `password_hash` - хеш пароля (bcrypt)
- `role` - роль (user/admin)
- `created_at` - дата создания
- `updated_at` - дата обновления

## 🚀 Запуск проекта

### Предварительные требования:
- Docker Desktop для Windows
- Docker Compose

### Команды для запуска:

```powershell
# Запустить все контейнеры
docker-compose up -d

# Проверить статус контейнеров
docker-compose ps

# Просмотреть логи
docker-compose logs -f

# Остановить все контейнеры
docker-compose down

# Остановить с удалением volumes (очистка БД)
docker-compose down -v
```

## 🌐 Доступ к сервисам

После запуска будут доступны:

- **Frontend**: http://localhost
- **Backend API**: http://localhost:8000
- **API Документация**: http://localhost:8000/docs
- **Adminer (БД)**: http://localhost:8080
  - Система: PostgreSQL
  - Сервер: postgres
  - Пользователь: postgres
  - Пароль: postgres
  - База данных: userdb
- **Redis статистика**: http://localhost:8000/api/redis/stats (или через админ-панель)

## 👤 Тестовый администратор

По умолчанию создается администратор:
- **Username**: admin
- **Password**: admin123
- **Email**: admin@example.com

## 📡 API Endpoints

### Пользователи:
- `GET /api/users` - получить всех пользователей
- `GET /api/users/{id}` - получить пользователя по ID
- `POST /api/register` - регистрация нового пользователя
- `POST /api/login` - вход пользователя
- `PUT /api/users/{id}` - обновить данные пользователя
- `DELETE /api/users/{id}` - удалить пользователя

### Примеры запросов:

**Регистрация:**
```json
POST /api/register
{
  "username": "testuser",
  "email": "test@example.com",
  "password": "password123"
}
```

**Вход:**
```json
POST /api/login
{
  "username": "testuser",
  "password": "password123"
}
```

## 🔧 Разработка

### Структура проекта:
```
user-management/
├── backend/
│   ├── app/
│   │   └── main.py          # FastAPI приложение
│   ├── Dockerfile           # Образ для backend
│   └── requirements.txt     # Зависимости Python
├── frontend/
│   ├── src/
│   │   ├── index.html       # Главная страница
│   │   ├── script.js        # Логика frontend
│   │   ├── style.css        # Стили
│   │   └── assets/          # Статические ресурсы (иконки, изображения и т.п.)
│   ├── Dockerfile           # Образ для frontend
│   └── nginx.conf           # Конфигурация Nginx
├── database/
│   └── init.sql             # Инициализация БД
├── .github/
│   └── workflows/
│       └── docker-build.yml # CI/CD конфигурация
├── README.md                # Документация проекта
└── docker-compose.yml       # Оркестрация контейнеров
```

### Локальная разработка:

Backend запускается с hot-reload, изменения применяются автоматически.

```powershell
# Пересобрать контейнеры после изменений
docker-compose up -d --build

# Посмотреть логи конкретного сервиса
docker-compose logs -f backend
```

## 🧪 CI/CD

GitHub Actions автоматически:
- Собирает Docker образы
- Запускает тесты
- Проверяет работоспособность сервисов

При пуше в `main` или `master` ветку запускается pipeline.

## 📝 Возможности системы

### Для всех пользователей:
- Регистрация и вход в систему
- Просмотр списка пользователей
- Безопасное хранение паролей

### Для администраторов:
- Удаление пользователей
- Доступ к Adminer для прямого управления БД
- Просмотр информации о БД

## 🛠️ Обоснование выбора технологий

### Docker & Docker Compose:
- Изоляция сервисов
- Простое развертывание
- Одинаковое окружение для разработки и продакшена
- Легкая масштабируемость

### PostgreSQL:
- Надежная реляционная СУБД
- Поддержка сложных запросов
- Отличная производительность
- Open-source

### FastAPI:
- Высокая производительность
- Автоматическая генерация документации
- Типизация (Pydantic)
- Асинхронность из коробки

### Nginx:
- Легкий и быстрый веб-сервер
- Статические файлы
- Reverse proxy возможности